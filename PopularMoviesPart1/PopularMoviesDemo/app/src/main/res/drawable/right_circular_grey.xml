<?xml version="1.0" encoding="utf-8"?>

<!-- This is used to customize the sort by rating button.
     It is used when the button is not pressed - selected -->

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="5dp"
        android:radius="1dp"
        android:topLeftRadius="0dp"
        android:topRightRadius="5dp" />
    <stroke
        android:width="1dp"
        android:color="@color/unpressed_button_dark" />
    <gradient
        android:angle="-90"
        android:endColor="@color/unpressed_button_light"
        android:startColor="@color/unpressed_button_dark" />
</shape>